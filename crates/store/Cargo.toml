[package]
name = "nexo-store"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "The client's encrypted local store. SQLCipher over a key that only ever exists wrapped by the OS keystore (brief 4.3)."

[dependencies]
nexo-platform.workspace = true
nexo-protocol.workspace = true
rusqlite.workspace = true
thiserror.workspace = true
zeroize.workspace = true
uuid.workspace = true
rand.workspace = true
# Only to keep the member list in one column. A child table would be tidier,
# but these are read and written together and never queried individually.
serde_json.workspace = true
